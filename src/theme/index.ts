import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      default: "#F2EBF0",
    },
    primary: {
      main: "#049DBF",
    },
  },
});

export default theme;
